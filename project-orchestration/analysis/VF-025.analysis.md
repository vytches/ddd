---
# Artefakt analizy — kontrakt handoff research → implementacja (ADR 0002).
# Pisany przez /analyze, edytowany przez CZŁOWIEKA, czytany przez /orchestrate.
task: VF-025
status: approved
stack_blocks: [ts-library, library-layers, nx-monorepo, approval-gate]
layers_done: [implementation, testing, api-surface]

threat_model: null # panel tego projektu nie ma stage'u threat-model (brak "ddd/core"/security bloku)

rag:
  skipped (project.yml nie ma knowledge_collection / runtime.yml nie ma sekcji
  knowledge — brak MCP RAG w tym repo)

open_questions:
  - id: Q1
    ask: >-
      To zadanie dotyka czterech różnych obszarów biblioteki i mocno różni się
      ryzykiem — od bezpiecznych poprawek po zmiany, które mogą po cichu
      przestawić zachowanie u dzisiejszych klientów. Czy zgadzasz się podzielić
      je na mały, bezpieczny pakiet do wydania teraz i osobne zadania na resztę,
      zamiast robić wszystko naraz w jednym wydaniu?
    q: >-
      Panel (ecc:architect) rekomenduje rozbicie VF-025 na epic + PR-y wg klasy
      ryzyka BC, nie wg pakietu. Proponowany podział: (A) events — dedup
      handlera + diagnostyka w catch, zero BC, PATCH; (C) projections — AC8
      (deleteAll→delete, one-liner, zero BC) + AC9 (checkpoint resume, opt-in)
      RAZEM (twarde sprzężenie — patrz Q4); (E) resilience — failureCount reset
      PRZY jednoczesnym dodaniu reguły "porażka w HALF_OPEN zawsze tripuje"
      (patrz Q2), izolowany PATCH; (G) cqrs — overwrite warning + typing (patrz
      Q6). DO OSOBNYCH TASKÓW: (B) flip autoRegisterHandlers opt-out→opt-in (BC,
      wymaga deprecation window); (D) AC7 retryConfig + AC10 error-propagation w
      projekcjach (behavioralny BC, wspólna migration note); (F) AC12+AC13
      resilience observability wire-or-drop (redesign, dziś martwy kod,
      najniższy priorytet). Jeśli task ma zostać PATCH-em, powyższy podział jest
      wymuszony — B/D/F się w patchu nie mieszczą. POTWIERDZONE przez wszystkich
      4 konsultowanych agentów (library-api-guardian, library-quality-verifier,
      library-expert, developer-experience) — jednomyślna zgoda.
      developer-experience: podział wg PR-ów w ramach JEDNEGO patch-release'a
      (A+C+E+G razem jako jedno wydanie, nie 4 osobne) minimalizuje "release
      fatigue" bez mieszania sygnału "bezpiecznie" z "przeczytaj migration
      guide". library-api-guardian: uzupełnienie — (E) jest bezpieczny TYLKO
      jeśli podczas code review nikt nie rozdzieli reset+reguła na dwa osobne
      PR-y "dla czytelności" (patrz Q2).
    answer: >-
      TAK. A+C+E+G jako jedno wydanie PATCH teraz. B, D, F jako osobne, nazwane
      zadania follow-up (proponowane oznaczenia: VF-025b — flip
      autoRegisterHandlers z deprecation window; VF-025c — AC7 retryConfig +
      AC10 error-propagation, wspólna migration note; VF-025d — AC12+AC13
      resilience observability wire-or-drop, najniższy priorytet).
  - id: Q2
    ask: >-
      Jedna z poprawek w mechanizmie ochronnym (circuit breaker) wygląda prosto,
      ale zrobiona samodzielnie POGORSZY odporność systemu zamiast ją poprawić.
      Czy zgadzasz się, żeby tę poprawkę wdrożyć wyłącznie razem z dodatkową
      regułą bezpieczeństwa, nigdy osobno?
    q: >-
      circuit-breaker.ts:150-176. Dziś: onFailure() nie sprawdza stanu,
      inkrementuje failureCount bez warunku; w HALF_OPEN failureCount jest już
      >= failureThreshold z poprzedniego trip, więc JEDNA porażka natychmiast
      re-tripuje (przypadkowo poprawne zachowanie, oparte o nie-wyzerowany
      licznik). AC10's "bonus" (resetować failureCount przy OPEN→HALF_OPEN,
      updateStateIfNeeded:171-176) W IZOLACJI to regresja: failureCount startuje
      od 0, jedna porażka daje failureCount=1, co tripuje TYLKO gdy
      failureThreshold===1 — inaczej obwód zostaje HALF_OPEN i przepuszcza
      kolejny ruch do zepsutej zależności. Wymagany fix: jawna reguła "dowolna
      porażka podczas HALF_OPEN natychmiast tripuje" wdrożona W TYM SAMYM commit
      co reset licznika. Potwierdzone czytaniem kodu (backend-technology-expert
      + własna weryfikacja). library-expert (niezależna weryfikacja):
      wykonalność ŁATWA — konkretny diff w onFailure() (rozbicie na gałąź `if
      state===HALF_OPEN → trip natychmiast` przed istniejącym warunkiem
      progowym), zero zmian w CircuitBreakerMetrics/publicznym API. Edge case
      potwierdzony czytaniem kodu: przy `halfOpenMaxProbes > 1` (kilka
      równoległych próbek), jeśli jedna failuje (trip→OPEN) a inna kończy się
      sukcesem w tym samym momencie, onSuccess() trafia w gałąź "else" i zeruje
      failureCount mimo że obwód jest świeżo OPEN — nie psuje przejść stanu (te
      sterowane wyłącznie przez nextAttemptTime), ale zafałszowuje
      getMetrics().failureCount na chwilę. Nie blokuje wdrożenia, ale wymaga
      testu na ten wyścig + komentarza w kodzie, żeby nie wyglądało na
      przeoczenie.
    answer: >-
      TAK, zgoda na oba elementy razem, jako jedna niepodzielna jednostka pracy
      w jednym PR/commit — z jawnym zastrzeżeniem, że podczas review nie wolno
      ich rozdzielić na osobne zmiany "dla czytelności". Dodać test na wyścig
      przy halfOpenMaxProbes>1 (metryka failureCount chwilowo błędna przy OPEN)
      jako świadomie udokumentowane zachowanie, nie przeoczenie.
  - id: Q3
    ask: >-
      Naprawa błędu w mechanizmie ponawiania prób przy budowaniu projekcji może
      sprawić, że część błędów, które dziś system sam naprawia w tle (np.
      chwilowa awaria sieci), zacznie od razu przerywać pracę. Czy chcesz,
      żebyśmy złagodzili tę zmianę tak, by nic dzisiaj działającego się nie
      zepsuło?
    q: >-
      projection-engine.ts:309 + error-strategy.ts:4-43. Dziś: limit prób JEST
      respektowany (pętla ma już `attempt < maxAttempts`), ale klasyfikacja
      błędu jest ignorowana — `shouldRetry` bez configu klasyfikuje po substring
      wiadomości (`isTransientError`). Gołe przekazanie `this.retryConfig`
      zmienia klasyfikację na `error.constructor.name` — `throw new
      Error('connection timeout')` dziś dostaje retry (message match), po fixie
      NIE (constructor.name==='Error', poza allowlistą). library-expert
      (niezależna weryfikacja, wykonalność ŁATWA/ŚREDNIA):
      `IProjectionRetryConfig.retryableErrors`/`nonRetryableErrors` to WYMAGANE
      (nie-opcjonalne) `string[]` w obu wariantach konstruktora
      EnhancedProjectionEngine — więc "nieskonfigurowany" ma jednoznaczną,
      strukturalną definicję (`retryableErrors.length===0 &&
      nonRetryableErrors.length===0`), bez dwuznaczności. Konkretny fix to DWIE
      zmiany razem: (1) projection-engine.ts:309 — dopisać `this.retryConfig`
      jako 3. argument (dziś w ogóle nie przekazywany, task file tego jawnie nie
      wspominał); (2) error-strategy.ts::isRetryableError() — gdy
      "nieskonfigurowany", dołożyć `isTransientError(error)` jako dodatkowy
      warunek `true` PRZED sprawdzeniem list domyślnych. Jeśli konsument ustawi
      choć jedną z list (np. tylko nonRetryableErrors), NIE jest to już
      "nieskonfigurowany" — superset-fallback nie wchodzi, ufamy w 100% jego
      configowi (defensywne i słuszne). developer-experience: mimo że nowy zbiór
      jest formalnie nadzbiorem dzisiejszego (bezpieczne), trzeba JAWNIE ostrzec
      w migration note że WIĘCEJ błędów będzie teraz retry'owanych — konsument
      polegający na fail-fast dla konkretnego message-substring dostanie dłuższą
      latencję zamiast szybkiego błędu. Zmiana niewidoczna w typach — brak
      sygnału z kompilatora/api-extractora.
    answer: >-
      TAK, wdrożyć wariant superset (A) — dwie zmiany razem: przekazać
      this.retryConfig w projection-engine.ts:309 ORAZ dołożyć
      isTransientError() jako dodatkowy warunek retry gdy config
      nieskonfigurowany (obie listy puste). Dodać JSDoc na polach
      retryableErrors/nonRetryableErrors w IProjectionRetryConfig wyjaśniający
      semantykę superset. Migration note MUSI jawnie ostrzec o możliwej
      zwiększonej latencji dla konsumentów polegających dziś na fail-fast. Dodać
      test właściwości "każdy błąd, który dziś dostaje retry, nadal go dostaje
      po fixie".
  - id: Q4
    ask: >-
      Wznawianie odbudowy danych od zapisanego punktu (zamiast zawsze od zera)
      jest możliwe, ale w pewnych warunkach może po cichu zdublować dane. Czy
      włączyć tę funkcję od razu wszystkim, czy zostawić jako opcję, którą ktoś
      świadomie włącza?
    q: >-
      checkpoint-capability.ts:27-28 zapisuje `position: 0` gdy zdarzenie nie
      niesie `metadata.position` w swoim number-typowanym polu (większość
      zdarzeń konsumenta go nie niesie) — ale zapisuje przy tym już zaawansowany
      `state`. Naiwny domyślny resume zasieje store stanem z pozycji N i
      odtworzy replay od 0 → podwójna aplikacja zdarzeń → cicha korupcja dla
      nieidempotentnego `apply()`. Dodatkowo: `position: number`
      (checkpoint-capability.ts:39) vs `IReplayFilter.fromPosition: bigint`
      (event-replay.interfaces.ts:23, potwierdzone istnieje) — wymaga jawnej
      konwersji z walidacją zakresu. library-expert (niezależna weryfikacja):
      zmiana `position` na `bigint` w CheckpointCapability TERAZ byłaby błędem
      zakresu — to pole jest częścią całego publicznego kontraktu
      IProjectionCheckpointStore (już opublikowanego w 0.31.0, używanego przez
      projection.builder.ts:withCheckpoints()), zmiana typu = MAJOR,
      nieproporcjonalne do "hardening patch". Most number↔bigint przy resume
      jest właściwym rozwiązaniem NA TERAZ, ale `BigInt(number)` jest bezpieczny
      tylko dla liczb w zakresie Number.MAX_SAFE_INTEGER (2^53-1) — wymaga
      jawnego `Number.isSafeInteger(checkpoint.position)` guard przed konwersją
      (realny edge case przy wieloletniej, wysokoprzepustowej instalacji),
      traktując przekroczenie jak niepoprawny checkpoint (fallback do pełnego
      rebuildu). Rekomendacja: resume opt-in (`resumeFromCheckpoint?: boolean`,
      default false na `IProjectionRebuildConfig`), z twardą walidacją
      monotoniczności pozycji — brak walidacji, `position <= 0`, LUB
      `!Number.isSafeInteger(position)` → odmowa resume + fallback do pełnego
      rebuildu + `internalLogger.warn`. Technicznie WYKONALNE bez zmiany
      kontraktu (rebuild() już async, getCapability()/loadCheckpoint() już
      istnieją). developer-experience: opt-in z default false to właściwy wybór
      DX (zero silent behavior change); migration note musi podać dokładny tekst
      komunikatu internalLogger.warn (żeby konsumenci mogli go
      grepować/monitorować) i jawnie wymagać idempotentnego apply() przed
      włączeniem.
    answer: >-
      TAK, opt-in, default false. Walidacja przy resume: odmowa (fallback do
      pełnego rebuildu + warn) gdy brak checkpointu, position<=0, LUB
      !Number.isSafeInteger(position). Dokładny tekst ostrzeżenia
      internalLogger.warn musi być ustalony i udokumentowany (grepowalny) w
      migration note razem z wymogiem idempotentnego apply().
  - id: Q5
    ask: >-
      Funkcja zbierania metryk odporności systemu (np. ile razy zadziałało
      zabezpieczenie przed przeciążeniem) jest dziś opublikowana konsumentom,
      ale w środku nic nie robi. Wolisz, żebyśmy ją dokończyli, czy usunęli z
      publicznego interfejsu (co wymaga dłuższego, wieloetapowego procesu
      wycofania, bo już jest opublikowana)?
    q: >-
      `enableMetrics?: boolean` (resilience-decorators.ts:10) nigdzie
      odczytywane; `*MetricCollector` klasy (CircuitBreaker/Bulkhead/Retry/
      Timeout) eksportowane publicznie z packages/resilience/src/index.ts, nigdy
      podłączone do CircuitBreaker/RetryPolicy/Bulkhead. Opcja (a) wire-up:
      sygnatury bez zmian, nowe obserwowalne zachowanie runtime → MINOR, bez
      deprecacji. Opcja (b) remove: to REMOVES już opublikowanych (0.31.0),
      realnie kompilujących się symboli — BC4 wymaga trójfazowej deprecacji
      niezależnie od tego, że nigdy nie działały (to NIE jest analogiczne do
      VF-036's getEqualityComponents, który nigdy nie istniał w źródle) → MAJOR
      jeśli usunięte od razu. POTWIERDZONE jednomyślnie przez
      library-api-guardian i developer-experience: (a) jest jedyną rozsądną
      opcją. developer-experience: usunięcie byłoby najbardziej frustrującym
      typem breaking change dla konsumenta ("zepsuliście mi coś, co i tak nic
      nie robiło"); dokończenie naprawia istniejącą niespójność UX (kod wygląda
      jakby działał, kompiluje się, ale jest martwy). Migration note MUSI
      explicite ostrzec: podane `*MetricCollector` zaczną realnie odbierać
      callbacki (trip/reject/attempt/timeout) — konsument z side-effectowym
      handlerem (zapisy do DB, alerty) musi to sprawdzić PRZED aktualizacją, nie
      po. Przy okazji odkryto NIEZALEŻNY od VF-025 gap: `getResilienceMetrics()`
      ma `@deprecated` JSDoc bez towarzyszącego `console.warn` w runtime
      (narusza PA6/BC4/N4 tego repo) — wart osobnego, małego tasku, nie blokuje
      VF-025, ale warto opisać w tej samej sekcji dokumentacji resilience żeby
      konsument widział oba fakty naraz.
    answer: >-
      TAK, wire-up (opcja a), MINOR. Migration note z explicit ostrzeżeniem o
      nowych side-effectach w konsumenckich *MetricCollector handlerach +
      before/after snippet. Osobny mały task (nie blokuje VF-025): dodać
      console.warn do @deprecated getResilienceMetrics(), udokumentować w tej
      samej sekcji co wire-up metryk.
  - id: Q6
    ask: >-
      Można poprawić typowanie w jednym z mechanizmów rejestracji handlerów na
      dwa sposoby: zmieniając istniejący sposób (co może zepsuć część obecnego
      kodu klientów) albo dodając nowy, bezpieczniejszy sposób obok starego.
      Który wolisz?
    q: >-
      enhanced-command-bus.ts:342-363 `register()`/`registerFactory()` mają
      `commandType: unknown`. Zawężenie na konkretny typ/generic constraint = z
      definicji MAJOR wg drzewa decyzyjnego BC tego repo (zwężenie typu
      parametru). library-quality-verifier (weryfikacja bezpośrednia, mocniej
      niż analogia z event busem): command bus JUŻ DZIŚ ma identyczny mechanizm
      dual-key co event bus — mapy `Map<Function | string, ...>` (linie
      109-113), register/registerFactory normalizują klucz string-lub- Function
      (linie 346, 359), a `resolveHandler` (linie 491, 503) czyta z fallbackiem
      `get(commandClass) ?? get(commandClass.name)`. To NIE jest teoretyczne
      ryzyko — zawężenie istniejącej sygnatury złamałoby realnie używaną, dziś
      działającą ścieżkę string-key. Rekomendacja library-api-guardian
      (potwierdzona): dodać nową metodę (`registerTyped<T extends
      ICommand>(...)`) obok, zostawiając `register()` nietkniętą — "adds new
      export" = MINOR bezpieczne, zero ryzyka dla istniejących konsumentów.
      developer-experience: samo dodanie metody to za mało dla dobrego DX — dwie
      podobne metody w autocomplete bez kontekstu mylą. Wymagane: JSDoc na
      `register()` jawnie kierujący do `registerTyped()` ("prefer this for
      compile-time safety; register() kept for dynamic/plugin registration"),
      registerTyped() jako domyślny przykład w README/LLMGUIDE, przykład
      side-by-side w examples/.
    answer: >-
      TAK, dodać registerTyped<T extends ICommand>() obok istniejącej
      register(), nie zawężać istniejącej sygnatury — potwierdzone jako
      konieczne (nie tylko ostrożne), bo register() ma dziś realnie używaną
      string-key ścieżkę, którą zawężenie by złamało. Wymagane razem z
      implementacją: JSDoc na register() kierujący do registerTyped(),
      registerTyped() jako domyślny przykład w LLMGUIDE, side-by-side przykład w
      examples/.

decisions:
  - id: D1
    topic: 'Kolejność wdrożenia wewnątrz wybranego zakresu'
    choice: >-
      Widoczność błędów PRZED zmianą zachowania: najpierw ProjectionProcessor
      przestaje cicho połykać błędy (AC9-error-part) i clearProjectionState
      przestaje kasować cudze projekcje (AC8), DOPIERO POTEM retryConfig zaczyna
      wpływać na klasyfikację (AC7, Q3).
    means: >-
      Gdyby kolejność była odwrotna, błąd wprowadzony poprawką do ponawiania
      prób byłby dla nikogo niewidoczny w polu — projekcje przestałyby się cicho
      aktualizować bez żadnego komunikatu.
    rationale: >-
      Potwierdzone przez backend-technology-expert: zmiana klasyfikacji w AC7
      nie ma sygnału kompilatora ani api-extractora i jest dziś maskowana przez
      to, że processor połyka wszystkie błędy. Odwrócenie kolejności usuwa
      maskowanie przed wprowadzeniem ryzykownej zmiany. UWAGA: AC9's
      error-propagation część (ProjectionProcessor przestaje połykać błędy)
      wylądowała w follow-up tasku VF-025c razem z AC7 (patrz Q1 answer) — D1
      obowiązuje jako kolejność WEWNĄTRZ VF-025c, nie między VF-025 (teraz) a
      VF-025c.
  - id: D2
    topic: 'AC8 — zakres pracy jest mniejszy niż w pliku taska'
    choice: >-
      Zamiast rozszerzać interfejs IProjectionStore, zmienić WYŁĄCZNIE call-site
      w clearProjectionState() z deleteAll() na już istniejącą
      delete(projectionName).
    means: >-
      Ta poprawka jest tańsza i bezpieczniejsza niż pierwotnie zakładano — nic w
      publicznym kontrakcie się nie zmienia.
    rationale: >-
      Zweryfikowano bezpośrednio: projection-interfaces.ts:20-26 —
      `delete(projectionName: string): Promise<void>` już istnieje na
      IProjectionStore (linia 23), obok deleteAll() (linia 24). Task file (linie
      72-73) opisuje to jako "rozszerzenie niełamiące", sugerując dodanie metody
      — nieaktualne, metoda już jest w kodzie od wcześniejszej pracy. Zero
      ryzyka BC (potwierdzone niezależnie przez architekta i
      library-api-guardian).
  - id: D3
    topic: 'AC1 — dedup handlerów: warn, nie twardy dedup'
    choice: >-
      Zamiast twardej deduplikacji po referencji funkcji, logować ostrzeżenie
      przy wykrytym duplikacie (wzorzec Node EventEmitter) i naprawić przy
      okazji getHandlers(), które już dziś myląco pokazuje mniej handlerów niż
      faktycznie się wywołuje.
    means: >-
      Ochrona przed przypadkowym podwójnym wywołaniem handlera bez ryzyka, że
      ktoś świadomie rejestrujący ten sam handler dwa razy straci połowę wywołań
      po cichu.
    rationale: >-
      backend-technology-expert: dedup po referencji łapie mało realnych
      przypadków (bind() tworzy nową funkcję za każdym razem; class-based
      handlery to wrappery tworzone per-rejestracja, docstring getHandlers
      unified-event-bus.ts:407-411 to potwierdza). Realny duplikat (ta sama
      klasa zarejestrowana dwa razy) łapie się lepiej po (instance, methodName),
      nie po referencji funkcji-wrappera. Dodatkowo: getHandlers()
      (unified-event-bus.ts:413-422) już dziś zwraca `new Set(...)`, czyli cicho
      kolapsuje duplikaty w warstwie diagnostycznej, mimo że dispatch wywołuje
      je oba — niespójność do naprawienia w tym samym miejscu.
  - id: D4
    topic:
      'AC13 — Symbol.for/globalThis wymaga wersjonowanego klucza i jawnej
      ochrony przed tree-shakingiem'
    choice: >-
      Jeśli AC13 wchodzi w zakres (spun off do VF-025d, patrz Q1) — klucz
      Symbol.for MUSI nieść numer wersji kontraktu (np.
      '@vytches/ddd-resilience.metric-registry.v1'), NIE goły identyfikator
      pakietu. Zapis do globalThis MUSI być leniwy (wyłącznie w getterze
      getInstance(), nigdy na top-level modułu) i jawnie oznaczony jako
      side-effectful w package.json, bo packages/resilience/package.json ma
      POTWIERDZONE "sideEffects": false — bez jawnego oznaczenia, bundler
      konsumenta może po cichu wytrzepać (tree-shake) moduł z zapisem, co cicho
      wyłącza cały fix dual-load.
    means: >-
      Bez wersji w kluczu dwie różne wersje biblioteki działające w jednym
      procesie klienta dzieliłyby się instancją o niekompatybilnym kształcie —
      zamiana jednej awarii na gorszą, cichszą. Bez jawnej ochrony przed
      tree-shakingiem fix może zostać po cichu wyłączony przez bundler
      konsumenta, dokładnie ten typ cichej regresji, przed którym reszta analizy
      ostrzega gdzie indziej.
    rationale: >-
      backend-technology-expert, wzorzec z @opentelemetry/api. Dodatkowo:
      getInstance() dziś zwraca konkretny typ DefaultMetricRegistry
      (metric-registry.ts:156) — musi zmienić się na strukturalny interfejs,
      inaczej fix jest bezużyteczny dla dual ESM/CJS load, ale to samo w sobie
      jest widoczną zmianą w api-report (drobny compile-time BC dla kogoś, kto
      zaadnotował zmienną konkretnym typem klasy — do ujęcia w changesetcie
      nawet jeśli reszta jest PATCH). `"sideEffects": false` POTWIERDZONE obecne
      w packages/resilience/package.json przez library-quality-verifier
      (weryfikacja bezpośrednia) — decyzja musi wybrać: (a) explicite wymienić
      plik z zapisem w tablicy sideEffects package.json, albo (b) zagwarantować
      że zapis następuje WYŁĄCZNIE przy jawnym wywołaniu getInstance(), nigdy
      przy samym imporcie modułu — rekomendacja: (b), spójne z resztą biblioteki
      (leniwa inicjalizacja), bez potrzeby wyjątków w sideEffects.

patterns:
  - cross-layer/conventions-pattern.md
  - typescript-library/public-api-pattern.md
  - typescript-library/package-boundary-pattern.md
  - typescript-library/build-publish-pattern.md
  - typescript-library/backward-compatibility-pattern.md

units: []
---

# Analiza: VF-025

## Synteza (tech-lead — fallback: główny agent, panel przerwany brakiem

kolejnego live leaf po trzech ukończonych stage'ach; pełny kontekst zachowany,
synteza zrobiona bez utraty informacji; DODATKOWA konsultacja czterech agentów
doradczych — library-api-guardian, library-quality-verifier, library-expert,
developer-experience — na wyraźną prośbę użytkownika, w celu wypełnienia pól
`answer:`)

VF-025 w obecnym kształcie miesza dwie różne klasy pracy pod jedną etykietą
"hardening": czyste bugfixy bez ryzyka (dedup handlerów, deleteAll→delete,
diagnostyka w catch) i zmiany, które po cichu przestawiają zachowanie
działającej dziś biblioteki u realnych konsumentów (flip autoRegisterHandlers,
klasyfikacja błędów w retry, zawężenie typu w CQRS). Publikacja v0.31.0 zmieniła
koszt tej drugiej kategorii z "tanio teraz" na "wymaga procesu wycofania" —
dlatego rekomendacja to rozbicie na mały, bezpieczny pakiet do wydania od razu i
osobne zadania na resztę (Q1), POTWIERDZONE jednomyślnie przez wszystkich
czterech konsultowanych agentów.

Weryfikacja kodu (własna + panel + dodatkowa konsultacja) obaliła część założeń
task file'a na korzyść — AC8 jest tańszy niż zakładano (metoda już istnieje w
interfejsie, D2) — i odkryła jedną poważną pułapkę, której task file nie
przewidywał: naiwna implementacja "bonusu" z AC10 (reset licznika błędów) w
izolacji POGARSZA odporność systemu, nie poprawia jej (Q2). To jedyne miejsce w
całej analizie, gdzie "oczywisty" fix jest błędny — library-expert potwierdził
wykonalność poprawnej wersji jako ŁATWĄ, z jednym dodatkowym edge case'em do
przetestowania (wyścig przy równoległych HALF_OPEN probach).

Wszystkie 6 pytań ma teraz rekomendowaną odpowiedź (pole `answer:` wypełnione) —
wynik zgodnej pracy panelu analizy i czterech dodatkowo konsultowanych agentów
specjalistycznych, bez sprzeczności między nimi. Pozostaje decyzja człowieka:
zaakceptować odpowiedzi wprost, poprawić, albo zakwestionować przed ustawieniem
`status: approved`.

## Otwarte pytania (DO DYSKUSJI — odpowiedzi wypełnione, do potwierdzenia lub

poprawy w frontmatter `answer:`)

- **Q1**: TAK — podział na PATCH teraz (A+C+E+G) + 3 osobne follow-up taski (B,
  D, F).
- **Q2**: TAK — reset licznika + reguła "HALF_OPEN zawsze tripuje" jako jedna
  niepodzielna jednostka.
- **Q3**: TAK — klasyfikacja błędów jako suma (superset), z jawnym ostrzeżeniem
  o możliwej zwiększonej latencji.
- **Q4**: TAK — wznawianie odbudowy opt-in, default false, z pełną walidacją (w
  tym Number.isSafeInteger).
- **Q5**: TAK — dokończyć metryki resilience (wire-up), nie usuwać.
- **Q6**: TAK — nowa metoda registerTyped() obok istniejącej, nie zawężać.

## Decyzje (proponowane — zweryfikuj)

- **D1**: Kolejność wdrożenia — widoczność błędów przed zmianą zachowania
  (obowiązuje wewnątrz follow-up tasku VF-025c).
- **D2**: AC8 to jednolinijkowa zmiana call-site, nie rozszerzenie interfejsu —
  task file nieaktualny.
- **D3**: Dedup handlerów jako warn, nie twardy dedup; naprawić przy okazji
  getHandlers().
- **D4**: Klucz Symbol.for musi być wersjonowany I jawnie chroniony przed
  tree-shakingiem (sideEffects:false potwierdzone w resilience) — dotyczy
  follow-up tasku VF-025d.

## Ryzyka / uwagi

- **Znaleziska poza pierwotnym zakresem VF-025, niezależne, niskiego ryzyka**:
  `getResilienceMetrics()` ma `@deprecated` bez `console.warn` w runtime
  (narusza wewnętrzny wzorzec BC tego repo) — warty osobnego, małego tasku, nie
  blokuje VF-025.
- **AC5 i AC14 z pierwotnego task file są już w pełni zrobione** — `publishMany`
  ma kompletną dokumentację semantyki (w tym różnic BaseEventBus vs
  UnifiedEventBus) w LLMGUIDE.md; ryzyko minifikacji `eventName` i wymóg jawnego
  `targetVersion` przy upcastingu też są już udokumentowane. Task file wymaga
  aktualizacji tych dwóch pozycji na "already-done" przed jakąkolwiek
  implementacją.
- **AC13's `globalPolicyEventBus` sub-item jest VOID** — ten singleton został
  całkowicie usunięty w VF-024; pozostały zakres AC13 to wyłącznie
  `GlobalMetricRegistry`/`GlobalObservabilityEventBus` w resilience.
- **Wszystkie trzy fakty pozostawione jako "do sprawdzenia przez implementera" w
  poprzedniej wersji tego pliku są teraz ZWERYFIKOWANE
  (library-quality-verifier, bezpośrednio w kodzie)**:
  1. Command bus MA fallback string↔Function przy lookupie
     (`enhanced-command- bus.ts:109-113, 491, 503`) — identyczny mechanizm co
     event bus, wcielone do Q6 jako mocniejszy dowód.
  2. `tsconfig.base.json` ma `"target": "ES2020", "lib": ["ES2020", "DOM"]` —
     **`AggregateError` (lib.es2021) NIE jest dostępny** w packages/projections
     i packages/resilience (dziedziczą bez override). To TWARDY BLOKER dla
     wariantu implementacji AC9's error-propagation opartego o natywny
     `AggregateError` w follow-up tasku VF-025c — implementer musi albo podnieść
     `lib` w tsconfig.base.json (dotyka całego monorepo, osobna decyzja), albo
     użyć własnej klasy zastępczej z polem `errors: Error[]`.
  3. `packages/resilience/package.json` MA `"sideEffects": false` — wcielone do
     D4 jako wymagana ochrona przed tree-shakingiem.
- Typ `position: number` (checkpoint) vs `fromPosition: bigint` (IReplayFilter)
  to konkretna przeszkoda techniczna dla AC9 (follow-up VF-025c), nie tylko
  ryzyko projektowe — rozwiązanie to jawny most z `Number.isSafeInteger` guard
  (wcielone do Q4), NIE zmiana typu `position` na `bigint` w całym publicznym
  kontrakcie `IProjectionCheckpointStore` (to byłby osobny MAJOR, poza
  zakresem).

---

> Po potwierdzeniu/poprawieniu odpowiedzi: ustaw `status: approved`, potem
> uruchom `/orchestrate VF-025`.
