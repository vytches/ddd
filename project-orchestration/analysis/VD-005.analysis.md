---
task: VD-005
status: approved
threat_model: null
rag:
  skipped (no .claude/config/knowledge.json present in this project — MCP
  knowledge-retriever not configured)
patterns: []
patterns_note: >
  Pattern discovery (step 0.5) was attempted but
  `.claude/knowledge/patterns/README.md` and
  `.claude/knowledge/patterns/_stack-defaults/typescript-library.yml` both
  returned "File does not exist" via Read (confirmed by two independent agents
  this session, main + Explore). CLAUDE.md's own "Patterns Library" table names
  5 files (public-api-pattern.md, backward-compatibility-pattern.md,
  package-boundary-pattern.md, library-testing-pattern.md,
  build-publish-pattern.md) but none resolved directly either. This session had
  NO Bash/Glob/Grep access at all (environment-wide, not per-agent) so the
  symlink/directory could not be inspected to determine if it's broken or
  genuinely absent. Low practical impact for VD-005 (docs/JSDoc/tooling task,
  not a domain-modeling task touching aggregate/VO/policy design), but flag for
  re-attempt once shell access is restored, since library-testing-pattern.md
  would ground AC11's CI-gate design.
open_questions:
  - id: OQ-1
    question: >
      Split VD-005 into VD-005a (documentation/config prose fixes, AC1-10,
      ~8-9h) and VD-005b (new "docs compile gate" tooling, AC11 only, ~10-14h) —
      mirroring the VD-006 -> VD-006a/VD-006b precedent (2026-07-04, same
      failure mode: a "simple/documentation" task silently containing greenfield
      tooling engineering)? Both panel agents (architect,
      library-quality-verifier) independently concluded AC11 is not
      documentation work: no markdown-code-fence-extraction tooling exists in
      this repo, and VD-006a (the task AC11 claims to "extend") actually
      AST-parses real examples/*/src/*.ts files, not markdown fences — a
      different mechanism entirely.
    answer: >
      No split — keep as one VD-005 task, all 11 ACs (human decision 2026-07-12:
      release timing is not a constraint here, do it all if it makes sense).
      Sequencing still applies within the single run: AC1-10 (prose/config) land
      first, AC11 (tooling) last — per D-4 and the AC11-depends-on-AC6+AC10
      point (a gate gone live before the drift it targets is fixed would
      immediately fail CI on its own target). Estimate ~18-23h combined, single
      branch/PR.
  - id: OQ-2
    question: >
      AC1: docs/README.md — rewrite from real repo state, or delete with a
      redirect to root README.md/QUICK_START.md? I read the current content
      directly this session: it is a "Documentation Structure" tree diagram +
      JSDoc-HTML generation instructions for the dead pnpm
      jsdoc:publish/serve/watch pipeline + a "LLM-Optimized Tags" section whose
      only content is a literal "(to be added)" placeholder + generic
      aspirational bullet points ("Comprehensive README", "Multiple examples per
      public API", etc.) that describe a documentation standard, not actual
      per-package content. It carries no unique navigation value beyond what
      root README/QUICK_START already provide.
    answer: >
      Delete + redirect (human accepted the finding: no unique navigational
      value beyond root README/QUICK_START). Implementer replaces docs/README.md
      with a short stub pointing at root README.md and QUICK_START.md.
  - id: OQ-3
    question: >
      AC10(d) / VF-032 AC6 both currently claim the same fix (removing the
      nonexistent `forRoot({ autoRegister })` reference from
      packages/nestjs/README_SIMPLE_INTEGRATION.md). Confirm VD-005(a) takes it
      (one-line doc deletion, ready now) and VF-032 AC6 is annotated "superseded
      — handled under VD-005 AC10d" so the not-yet-started VF-032 doesn't re-do
      or conflict with it.
    answer: >
      VD-005 takes it now. Implementer adds a one-line note to
      project-orchestration/tasks/VF-032-nestjs-fluency.md AC6: "autoRegister
      reference removed under VD-005 AC10d (2026-07-12) — this AC's remaining
      scope is the forRootAsync/forFeature design work only."
  - id: OQ-4
    question: >
      AC10(e) claims packages/validation/LLMGUIDE.md:~358 references the deleted
      `@vytches/ddd-logging` package. A scoped Explore-agent scan of lines 1-30
      and 345-369 found no such reference this session (full-file scan wasn't
      possible — no grep access). Keep AC10(e) in scope pending a full-file
      check at implementation time, or drop it as already-stale?
    answer: >
      Keep in scope. Implementer does a full-file read/grep of
      packages/validation/LLMGUIDE.md for `@vytches/ddd-logging` during AC10
      work (shell access should be available inside the implementation run); if
      genuinely absent, mark AC10(e) done-as-no-op in the completion note rather
      than silently dropping it.
  - id: OQ-5
    question: >
      AC3 (ADR index regeneration) — root package.json already has
      `adr:generate` wired to `npx madr generate` (plus
      `adr:list`/`adr:status`), found this session but not previously known to
      the task file or either panel agent. Before assuming new scripting is
      needed: run `pnpm adr:generate` and check whether its output already
      matches the docs/adr/README.md index format (numbered list, title,
      filename link) used today. If yes, AC3 shrinks to "run existing command +
      spot check", not new tooling.
    answer: >
      Try `pnpm adr:generate` first. If its output format matches (or can be
      made to match via config) the current docs/adr/README.md index style, use
      it and drop the "consider a generating script" ambiguity from the AC —
      this is now the default path, not a fallback. Only hand-write/build new
      tooling if `adr:generate` proves unusable (e.g. wrong format, no config
      surface).
  - id: OQ-6
    question: >
      docs/guides/ already exists as a directory (confirmed: Read returned
      EISDIR) but its contents could not be enumerated this session (no
      directory-listing tool available). Before writing AC7's new "Specification
      vs Policy" guide, confirm no existing file in docs/guides/ already covers
      this or would collide on filename.
    answer: >
      Implementer lists docs/guides/ at the start of AC7 work (shell access
      should be available inside the implementation run) before creating the new
      file; if a colliding/overlapping guide already exists, extend it instead
      of creating a duplicate.
decisions:
  - id: D-1
    decision: >
      AC6 JSDoc bar: pure accessors (getVersion/getInitialVersion/hasChanges)
      get a one-line prose description above @returns — @example NOT required.
      apply() gets both @throws {Error} (stating the maxEvents trigger
      condition) AND a runnable @example demonstrating the guard. Do not
      retrofit @example onto other untouched public methods (scope creep for a
      cleanup task) — only new/touched methods.
    rationale: library-quality-verifier stage recommendation, unopposed.
    propose_adr: false
  - id: D-2
    decision: >
      AC11 scope (implemented as the final layer of the single VD-005 run, per
      OQ-1): opt-in marker syntax for compile-checked fences (e.g. a sentinel
      like ```ts compile or a leading `// compile-check` comment), not
      hard-fail-on-every-fence. Broaden to
      default-on-for-fences-importing-real-@vytches/ddd-*-symbols only after the
      opt-in version's false-positive rate is known. New tool needs its own L1
      (fence extractor: multi-file, multi-fence, marker detection) + L2 (fixture
      with one intentionally-broken fence, proves CI actually fails) tests
      before being trusted as a gate, at roughly the rigor of tools/ddd-lint's
      existing suite. Lives under a new tools/ package (own CLI + --check mode),
      following the tools/ddd-lint and tools/example-matrix structural
      precedent.
    rationale:
      library-quality-verifier stage; independently converges with architect's
      "AC11 must land after AC6+AC10 fixes, else it fails CI immediately on the
      very drift it's meant to catch" sequencing point.
    propose_adr: false
  - id: D-3
    decision: >
      AC5's "Result<T,E> — zgodnie z design decision biblioteki" has no
      dedicated ADR: checked docs/adr/README.md's full 0001-0019 index directly
      (no title matches), and the real precedent is completed task
      VF-017-result-pattern-standardization.md (2026-03-31, "Result<T,E> Pattern
      Standardization + Constructor Cleanup"), not a numbered ADR. Cite VF-017
      in VD-005's quickstart-reconciliation work. Separately: given VF-023
      (2026-07-11) extended this same convention into a BREAKING
      constructor-validation change for BaseValueObject/AggregateRoot, this
      load-bearing convention arguably deserves a real ADR now — proposing one,
      but as a candidate follow-up, not blocking VD-005's docs fix.
    rationale:
      direct verification against docs/adr/README.md contents +
      completed-tasks/VF-017.
    propose_adr: true
  - id: D-4
    decision: >
      Sequencing within the single VD-005 run: AC1 (docs/README.md) before AC5
      (quickstart reconciliation) — AC1's real-package-list/convention pass is
      the source AC5 aligns QUICK_START.md against. AC4 (dead ESLint
      scope:logging tag) and AC8 (SECURITY-AUDIT-2026-05-26.md logging section)
      both stem from the same VS-013 removal — batch them in one pass. AC2/AC9
      (abandoned-roadmap flags, CLAUDE.md regen) are independent, low-risk, do
      anytime. AC11 last (depends on AC6+AC10 being fixed first).
    rationale: architect stage recommendation.
    propose_adr: false
---

# VD-005 — Dokumentacja: usunięcie fikcji i uzgodnienie z kodem

## Synteza

Task VD-005 to jedyny **pełny, bez-warunkowych-zależności** task w aktywnym
backlogu z twardym `release_target: pre-first-public-publish` — stąd wybór go
jako pierwszego do zamknięcia przed cięciem release branch. Weryfikacja
source-code (nie tylko treści zadania) potwierdziła **wszystkie 10 z 11 AC jako
wciąż aktualne** — żadne nie okazało się już naprawione przez wcześniejsze taski
(VF-023/024/031 dotknęły inne warstwy: barrel exports, konstruktory
VO/AggregateRoot, nie README/JSDoc-prosa).

**Scope taska**: AC11 ("Gate kompilacji docs") to **nowa inżynieria
narzędziowa** (markdown-code-fence extractor + type-checker, własny CLI pod
`tools/`), nie edycja prozy — ten sam wzorzec, który `/analyze-ddd VD-006` już
raz wykrył (VD-006 → VD-006a/b, 2026-07-04). Panel (architekt +
library-quality-verifier, niezależnie od siebie) rekomendował split; **człowiek
zdecydował inaczej (2026-07-12, OQ-1): zostajemy przy jednym tasku,
release-timing nie jest tu ograniczeniem.** AC11 realizowany jako ostatnia
warstwa w tym samym przebiegu `/orchestrate-ddd`, po AC1-10 — kolejność
wymuszona i tak przez D-4 (gate uruchomiony przed naprawą AC6/AC10 zawaliłby się
na własnym celu). Łączny szacunek ~18-23h, jeden branch/PR.

Wszystkie 5 punktowych claimów AC10 (nie-kompilujące się snippety) zostały
**zweryfikowane bezpośrednio wobec prawdziwego kodu źródłowego** (nie tylko
wobec treści README) — patrz "Weryfikacja AC10" niżej. Jeden sub-punkt (AC10e)
pozostaje niepotwierdzony w tej sesji (OQ-4) — implementer douweryfikuje z
dostępem do shella.

Dwie kolizje międzytaskowe znalezione i rozstrzygnięte: (1) AC10(d) pokrywał się
dosłownie z VF-032 AC6 (ta sama poprawka `autoRegister`) — **VD-005 przejmuje,
VF-032 adnotowany jako superseded** (OQ-3); (2) AC3 (regeneracja indeksu ADR)
prawdopodobnie prawie darmowa dzięki istniejącemu `pnpm adr:generate` (madr) —
**spróbować najpierw, zanim ktokolwiek napisze nowy skrypt** (OQ-5).

**Uwaga proceduralna:** sesja analizy nie miała dostępu do Bash/Glob/Grep (całe
środowisko, nie tylko pojedynczy agent — potwierdzone niezależnie przez główny
proces i przez dedykowanego Explore-agenta). Wszystkie fakty poniżej pochodzą z
bezpośrednich odczytów (`Read`) konkretnych, znanych ścieżek plików — nie z
przeszukiwania katalogów. Tam, gdzie to ograniczenie uniemożliwiło pełną
weryfikację (pełna lista plików `docs/adr/`, zawartość `docs/guides/`, zawartość
`.claude/knowledge/patterns/`), zaznaczono to jawnie jako zadanie dla
implementera (OQ-4, OQ-6), nie jako ustalony fakt. Synteza tego etapu wykonana
bezpośrednio przez głównego agenta (nie osobny `@tech-lead` leaf) — oba stage'e
panelu zbiegły się bez sprzeczności wymagającej arbitrażu, więc dodatkowy leaf
niósłby tylko koszt bez nowej wartości (dozwolony fallback z sekcji "Hardening"
tej komendy).

## Weryfikacja AC10 (przeciw prawdziwemu kodowi źródłowemu, nie tylko README)

| Sub-AC | Claim                                                                                         | Status                                                                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 10a    | `packages/events/README.md` — zła kolejność argumentów `super(name, payload, meta)`           | **POTWIERDZONE** — realny konstruktor `DomainEvent` to `(payload?, metadata?, eventName?)` (`domain-event.ts:45`), payload-first. README konsekwentnie używa złej kolejności w obu przykładach. |
| 10b    | `packages/validation/README.md` — `.must()` nie istnieje                                      | **POTWIERDZONE** — `BusinessRuleValidator` (`business-rule-validator.ts`) ma `addRule/addSpecification/mustSatisfy/propertyMustSatisfy/addNested`, brak gołego `.must()`.                       |
| 10c    | `packages/resilience/LLMGUIDE.md` — złe nazwy pól (`baseDelayMs`, `maxConcurrent`/`maxQueue`) | **POTWIERDZONE** — realne pola to `baseDelay` (`retry.ts:6`), `maxConcurrency`/`queueCapacity` (`bulkhead.ts:4-5`).                                                                             |
| 10d    | `packages/nestjs/README_SIMPLE_INTEGRATION.md` — `forRoot({ autoRegister })` nie istnieje     | **POTWIERDZONE** — `VytchesDDDModuleOptions` (`types.ts`) nie ma pola `autoRegister`. Kolizja z VF-032 AC6 — rozstrzygnięta w OQ-3.                                                             |
| 10e    | `packages/validation/LLMGUIDE.md:~358` — referencja do usuniętego `@vytches/ddd-logging`      | **NIEPOTWIERDZONE** — do douweryfikowania przy implementacji (OQ-4).                                                                                                                            |

## Otwarte pytania — rozstrzygnięcia (2026-07-12)

_(pełna treść odpowiedzi w frontmatter — pola `answer:` powyżej)_

- **OQ-1** — brak splitu, jeden task, AC11 jako ostatnia warstwa.
- **OQ-2** — delete + redirect dla `docs/README.md`.
- **OQ-3** — VD-005 przejmuje AC10(d); VF-032 AC6 adnotowany jako superseded.
- **OQ-4** — zostaje w scope; pełna weryfikacja przy implementacji.
- **OQ-5** — najpierw `pnpm adr:generate`, nowy skrypt tylko jeśli się nie
  sprawdzi.
- **OQ-6** — implementer sprawdza `docs/guides/` przed dopisaniem nowego
  przewodnika.

## Decyzje (proponowane)

_(pełne uzasadnienia w frontmatter `decisions[]`)_

- **D-1** — AC6: bar dla JSDoc/`@example` (accessor = prosa bez example;
  `apply()` = `@throws`+`@example`; brak retrofit na nietknięte metody).
- **D-2** — AC11: opt-in marker dla compile-checked fences, nie hard-fail-all;
  własne testy L1+L2; nowy pakiet pod `tools/`.
- **D-3** — AC5: cytować `VF-017-result-pattern-standardization.md` (nie ADR —
  żaden nie istnieje); zaproponować nowy ADR jako osobny follow-up, nie
  blokujący.
- **D-4** — Kolejność: AC1 → AC5; AC4+AC8 batch (oba VS-013); AC2/AC9
  independent; AC11 ostatnia.

## Referencje

- Task: `project-orchestration/tasks/VD-005-docs-truth-cleanup.md`
- Analysis źródłowa:
  `project-orchestration/analysis/LIB-AUDIT-2026-07-02.analysis.md` (F-H16,
  F-M11, F-M12, F-M17)
- Analysis źródłowa: `LIB-UX-AUDIT-2026-07-10.analysis.md` (UX-T3)
- Precedens (rozważony, ostatecznie nie zastosowany):
  `project-orchestration/analysis/VD-006-example-coverage-matrix.analysis.md`
- `completed-tasks/VD-006a-example-matrix-generator.md` — mechanizm, który AC11
  błędnie zakłada jako bezpośrednio rozszerzalny
- `completed-tasks/VF-017-result-pattern-standardization.md` — realne źródło
  konwencji Result<T,E> (AC5)
- `tasks/VF-032-nestjs-fluency.md` — kolizja AC6 z VD-005 AC10(d),
  rozstrzygnięta w OQ-3
