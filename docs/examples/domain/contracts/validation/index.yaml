# Validation Index - Validation System Contracts Exports

# === FILE METADATA ===
file-name: 'index'
title: 'Validation Contracts Package Exports'
description: 'Validation system contracts and specification pattern exports'
business-context: 'Provides unified access to validation contract definitions'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'file'

# === FILE-LEVEL METADATA ===
file-doc:
  description: 'Central export hub for validation contracts and specifications'
  business-context: 'Foundation for business rule validation and specification pattern'
  
  formats:
    jsdoc:
      description: 'Validation contracts and specification interfaces'
    cli:
      description: |
        ## Validation Contracts Exports
        
        Validation system including:
        - Specification pattern interfaces
        - Validator contracts
        - Validation error definitions

  custom-tags:
    since: '1.0.0'
    stability: 'stable'
    category: 'validation'
    pattern: 'specification-pattern'
    
# === EXPORTS DOCUMENTATION ===
exports:
  specification-interfaces:
    description: 'Specification pattern interfaces for business rules'
    business-context: 'Contracts for implementing specification pattern validation'
    source: './specification.interfaces'
    type: 'namespace'
    
  validator-interfaces:
    description: 'Validator contracts for validation implementations'
    business-context: 'Interfaces for creating validators and validation rules'
    source: './validator.interfaces'
    type: 'namespace'