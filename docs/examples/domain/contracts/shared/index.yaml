# Shared Index - Shared Types Contracts Exports

# === FILE METADATA ===
file-name: 'index'
title: 'Shared Contracts Package Exports'
description: 'Shared types and interfaces exports'
business-context: 'Provides unified access to shared contract definitions'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'file'

# === FILE-LEVEL METADATA ===
file-doc:
  description: 'Central export hub for shared types and interfaces'
  business-context: 'Common contracts used across multiple domains'

  formats:
    jsdoc:
      description: 'Shared types and interfaces'
    cli:
      description: |
        ## Shared Contracts Exports

        Shared types including:
        - Snapshot types and interfaces
        - Common type definitions

  custom-tags:
    since: '1.0.0'
    stability: 'stable'
    category: 'shared-types'

# === EXPORTS DOCUMENTATION ===
exports:
  snapshot-types:
    description: 'Snapshot-related type definitions and interfaces'
    business-context: 'Contracts for aggregate snapshot functionality'
    source: './snapshot-types'
    type: 'namespace'
