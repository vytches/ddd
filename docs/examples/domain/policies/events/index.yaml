# events/index - Universal Enhanced Metadata System V2

# === FOLDER METADATA ===
folder-name: 'events'
title: 'Policy Events'
description: 'Event-driven policy components'
business-context: 'Enables policy observability and audit trails'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'folder'

# === EXPORTS ===
exports:
  classes:
    - 'EventDrivenPolicy'
    - 'PolicyEvaluationEvent'
    - 'PolicyEvaluatedEvent'
    - 'PolicyViolationEvent'
    - 'PolicyEventBus'

  interfaces:
    - 'IPolicyEventBus'
    - 'PolicyEventData'
