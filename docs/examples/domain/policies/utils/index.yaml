# utils/index - Universal Enhanced Metadata System V2

# === FOLDER METADATA ===
folder-name: 'utils'
title: 'Policy Utilities'
description: 'Helper utilities for policies'
business-context: 'Simplifies common policy operations'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'folder'

# === EXPORTS ===
exports:
  classes:
    - 'PolicyContextBuilder'
    - 'PolicyRequestBuilder'
