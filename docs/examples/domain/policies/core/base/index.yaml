# base/index - Universal Enhanced Metadata System V2

# === FOLDER METADATA ===
folder-name: 'base'
title: 'Base Policy Classes'
description: 'Abstract base classes for policies'
business-context: 'Foundation for policy implementation'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'folder'

# === EXPORTS ===
exports:
  classes:
    - 'BaseBusinessPolicy'
    - 'CompositeBusinessPolicy'
