# interfaces/index - Universal Enhanced Metadata System V2

# === FOLDER METADATA ===
folder-name: 'interfaces'
title: 'Policy Interfaces'
description: 'Core policy interfaces'
business-context: 'Contracts for policy implementation'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'folder'

# === EXPORTS ===
exports:
  interfaces:
    - 'IBusinessPolicy'
    - 'IPolicyRegistry'
    - 'PolicyRequest'
    - 'PolicyContext'
