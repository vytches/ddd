# Package Settings - Projections

# === PACKAGE IDENTITY ===
package-name: 'projections'
display-name: 'Event Projections'
description: 'Event projection patterns for read models and CQRS query side'
business-context: 'Transform event streams into optimized read models for queries'

# === HIERARCHICAL STRATEGY ===
hierarchy:
  strategy: 'merge'
  scope: 'package'

# === PACKAGE METADATA ===
domain: 'event-sourcing'
complexity: 'advanced'
patterns:
  - 'event-projection'
  - 'read-model'
  - 'event-replay'
  - 'checkpoint-pattern'
  - 'snapshot-pattern'

# === INTEGRATION ===
integration:
  event-store: 'Consumes events from event stores'
  capabilities: 'Extensible capability system'
  error-handling: 'Retry and circuit breaker patterns'
  rebuilding: 'Event replay and projection rebuilding'

# === ENTERPRISE FEATURES ===
enterprise-features:
  - 'Capability-based architecture'
  - 'Checkpoint management'
  - 'Snapshot optimization'
  - 'Dead letter queue'
  - 'Circuit breaker protection'
  - 'Incremental rebuilding'
  - 'Error recovery strategies'
