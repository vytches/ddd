# Sagas Repository Index - Universal Enhanced Metadata System V2

# === FILE METADATA ===
file-name: 'index'
title: 'Sagas Repository - Export Module'
description: 'Re-exports saga repository implementations'
business-context: 'Saga persistence layer'

# === HIERARCHY ===
hierarchy:
  strategy: 'merge'
  scope: 'file'

# === EXPORTS ===
exports:
  description: 'Saga repository exports'
  items:
    - from: './in-memory-saga-repository'
      exports: ['InMemorySagaRepository']
