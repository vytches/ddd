{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@vytches-ddd/value-objects",
  "projectType": "library",
  "sourceRoot": "packages/value-objects/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "vite build",
        "cwd": "packages/value-objects"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run packages/value-objects/tests --reporter=verbose"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/value-objects/**/*.ts"]
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project packages/value-objects/tsconfig.json"
      }
    }
  },
  "tags": ["type:lib", "scope:value-objects", "layer:foundation"]
}
