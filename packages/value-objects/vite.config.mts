// Value objects package - pattern layer (depends on domain-primitives)
import { createPatternConfig } from '../utils/build-configs';
export default createPatternConfig(__dirname, {
  jsdocExamples: { enabled: true, verbose: true },
});
