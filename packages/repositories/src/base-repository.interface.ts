// Re-export from contracts for backward compatibility
// Note: These interfaces are now defined in @vytches/ddd-contracts to avoid circular dependencies
export type {
  IRepository,
  IRepositoryProvider,
  IExtendedRepository,
  IRepositoryEntity,
} from '@vytches/ddd-contracts';
