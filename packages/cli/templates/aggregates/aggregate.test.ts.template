/**
 * @fileoverview {{name}} Aggregate Tests
 * Domain: {{domain}}
 * Generated by VytchesDDD CLI
 */

import { describe, it, expect, beforeEach } from 'vitest';
import { safeRun } from '@vytches/ddd-utils';
import { EntityId } from '@vytches/ddd-core';
import { {{name}} } from '../{{fileName}}.aggregate';
{{#if events}}
import { {{eventName}} } from '../events/{{eventFileName}}';
{{/if}}

describe('{{name}} Aggregate', () => {
  let aggregateId: EntityId;
  {{#each properties}}
  let {{name}}: {{type}};
  {{/each}}

  beforeEach(() => {
    aggregateId = EntityId.generate();
    {{#each properties}}
    {{name}} = {{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}};
    {{/each}}
  });

  describe('create', () => {
    it('should create {{name}} aggregate successfully', () => {
      const [error, aggregate] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      expect(aggregate).toBeDefined();
      expect(aggregate?.id).toBeDefined();
      {{#each properties}}
      expect(aggregate?.{{name}}).toBe({{name}});
      {{/each}}
    });

    {{#each validations}}
    it('should validate {{description}}', () => {
      // Test validation rule: {{rule}}
      const [error] = safeRun(() => 
        {{../name}}.create(
          {{#each ../properties}}
          {{#if (eq name @root.validationProperty)}}invalidValue{{else}}{{name}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeDefined();
      expect(error?.message).toContain('{{message}}');
    });
    {{/each}}

    {{#if events}}
    it('should raise {{eventName}} when created', () => {
      const [error, aggregate] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      expect(aggregate).toBeDefined();
      
      const domainEvents = aggregate?.getUncommittedEvents();
      expect(domainEvents).toHaveLength(1);
      expect(domainEvents?.[0]).toBeInstanceOf({{eventName}});
    });
    {{/if}}
  });

  describe('fromPersistence', () => {
    it('should reconstitute aggregate from persistence', () => {
      const [error, aggregate] = safeRun(() => 
        {{name}}.fromPersistence(
          aggregateId,
          {{#each properties}}
          {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      expect(aggregate).toBeDefined();
      expect(aggregate?.id).toBe(aggregateId);
      {{#each properties}}
      expect(aggregate?.{{name}}).toBe({{name}});
      {{/each}}
    });
  });

  {{#each methods}}
  describe('{{name}}', () => {
    let aggregate: {{../name}};

    beforeEach(() => {
      aggregate = {{../name}}.fromPersistence(
        aggregateId,
        {{#each ../properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );
    });

    it('should {{description}}', () => {
      const [error{{#unless (eq returnType 'void')}}, result{{/unless}}] = safeRun(() => 
        aggregate.{{name}}(
          {{#each parameters}}
          {{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      {{#unless (eq returnType 'void')}}
      expect(result).toBeDefined();
      {{/unless}}
    });

    {{#each validations}}
    it('should validate {{description}}', () => {
      const [error] = safeRun(() => 
        aggregate.{{../name}}(
          {{#each ../parameters}}
          {{#if (eq name @root.validationParameter)}}invalidValue{{else}}{{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeDefined();
      expect(error?.message).toContain('{{message}}');
    });
    {{/each}}

    {{#if raisesEvent}}
    it('should raise {{eventName}} when {{name}} is called', () => {
      const [error] = safeRun(() => 
        aggregate.{{name}}(
          {{#each parameters}}
          {{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      
      const domainEvents = aggregate.getUncommittedEvents();
      expect(domainEvents.length).toBeGreaterThan(0);
      
      const targetEvent = domainEvents.find(event => event instanceof {{eventName}});
      expect(targetEvent).toBeDefined();
    });
    {{/if}}
  });
  {{/each}}

  describe('toSnapshot', () => {
    it('should create snapshot for persistence', () => {
      const aggregate = {{name}}.fromPersistence(
        aggregateId,
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const snapshot = aggregate.toSnapshot();

      expect(snapshot).toBeDefined();
      expect(snapshot.id).toBe(aggregateId.value);
      {{#each properties}}
      expect(snapshot.{{name}}).toBe({{name}});
      {{/each}}
      expect(snapshot.version).toBeDefined();
      expect(snapshot.createdAt).toBeDefined();
      expect(snapshot.updatedAt).toBeDefined();
    });
  });

  describe('business invariants', () => {
    it('should maintain aggregate consistency', () => {
      const aggregate = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      // Test aggregate invariants
      expect(aggregate.id).toBeDefined();
      expect(aggregate.version).toBe(0);
      expect(aggregate.createdAt).toBeDefined();
      expect(aggregate.updatedAt).toBeDefined();
    });

    {{#if hasSpecifications}}
    it('should satisfy business rules', () => {
      const aggregate = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      // Test business rule specifications
      // Add specific business rule tests here
      expect(aggregate).toBeDefined();
    });
    {{/if}}
  });

  describe('event handling', () => {
    {{#if events}}
    it('should handle uncommitted events correctly', () => {
      const aggregate = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const uncommittedEvents = aggregate.getUncommittedEvents();
      expect(uncommittedEvents.length).toBeGreaterThan(0);

      aggregate.markEventsAsCommitted();
      const eventsAfterCommit = aggregate.getUncommittedEvents();
      expect(eventsAfterCommit).toHaveLength(0);
    });
    {{/if}}

    it('should track version correctly', () => {
      const aggregate = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const initialVersion = aggregate.version;
      
      {{#if methods}}
      // Perform a business operation
      {{#each methods}}
      {{#if @first}}
      const [error] = safeRun(() => 
        aggregate.{{name}}(
          {{#each parameters}}
          {{#if (eq type 'string')}}'test-value'{{else}}{{#if (eq type 'number')}}456{{else}}{{#if (eq type 'boolean')}}false{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );
      expect(error).toBeNull();
      {{/if}}
      {{/each}}
      {{/if}}

      expect(aggregate.version).toBe(initialVersion);
    });
  });
});