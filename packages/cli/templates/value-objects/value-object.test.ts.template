/**
 * @fileoverview {{name}} Value Object Tests
 * Domain: {{domain}}
 * Generated by VytchesDDD CLI
 */

import { describe, it, expect, beforeEach } from 'vitest';
import { safeRun } from '@vytches-ddd/utils';
import { {{name}} } from '../{{fileName}}.value-object';

describe('{{name}} Value Object', () => {
  {{#each properties}}
  let {{name}}: {{type}};
  {{/each}}

  beforeEach(() => {
    {{#each properties}}
    {{name}} = {{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}};
    {{/each}}
  });

  describe('create', () => {
    it('should create {{name}} value object successfully', () => {
      const [error, valueObject] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      expect(valueObject).toBeDefined();
      {{#each properties}}
      expect(valueObject?.{{name}}).toBe({{name}});
      {{/each}}
    });

    {{#if hasValidation}}
    it('should validate value object properties', () => {
      const [error] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{#if @first}}null as any{{else}}{{name}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeDefined();
      expect(error?.message).toContain('validation');
    });
    {{/if}}
  });

  describe('fromPrimitives', () => {
    it('should create from primitive values', () => {
      const [error, valueObject] = safeRun(() => 
        {{name}}.fromPrimitives({
          {{#each properties}}
          {{name}}: {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        })
      );

      expect(error).toBeNull();
      expect(valueObject).toBeDefined();
      {{#each properties}}
      expect(valueObject?.{{name}}).toBe({{name}});
      {{/each}}
    });
  });

  {{#if hasComparison}}
  describe('equality', () => {
    it('should be equal when all properties are the same', () => {
      const valueObject1 = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const valueObject2 = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      expect(valueObject1.equals(valueObject2)).toBe(true);
    });

    it('should not be equal when properties are different', () => {
      const valueObject1 = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const valueObject2 = {{name}}.create(
        {{#each properties}}
        {{#if @first}}{{#if (eq type 'string')}}'different-{{name}}'{{else}}{{#if (eq type 'number')}}456{{else}}{{#if (eq type 'boolean')}}false{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{else}}{{name}}{{/if}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      expect(valueObject1.equals(valueObject2)).toBe(false);
    });
  });
  {{/if}}

  describe('immutability', () => {
    it('should be immutable', () => {
      const valueObject = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      // Verify properties are readonly
      {{#each properties}}
      expect(() => {
        (valueObject as any).{{name}} = {{#if (eq type 'string')}}'modified'{{else}}{{#if (eq type 'number')}}999{{else}}{{#if (eq type 'boolean')}}false{{else}}{}{{/if}}{{/if}}{{/if}};
      }).not.toThrow();
      // Property should remain unchanged due to readonly
      expect(valueObject.{{name}}).toBe({{name}});
      {{/each}}
    });
  });

  describe('toPrimitives', () => {
    it('should convert to primitive values', () => {
      const valueObject = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const primitives = valueObject.toPrimitives();

      expect(primitives).toBeDefined();
      {{#each properties}}
      expect(primitives.{{name}}).toBe({{name}});
      {{/each}}
    });
  });

  describe('toString', () => {
    it('should provide string representation', () => {
      const valueObject = {{name}}.create(
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const stringRep = valueObject.toString();

      expect(stringRep).toBeTypeOf('string');
      expect(stringRep.length).toBeGreaterThan(0);
      {{#each properties}}
      expect(stringRep).toContain(String({{name}}));
      {{/each}}
    });
  });

  {{#if hasValidation}}
  describe('validation', () => {
    it('should validate all properties', () => {
      // Test specific validation rules
      const [error] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{#if (eq type 'string')}}''{{else}}{{#if (eq type 'number')}}-1{{else}}{{#if (eq type 'boolean')}}false{{else}}null as any{{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeDefined();
    });
  });
  {{/if}}
});