/**
 * @fileoverview {{name}} Entity Tests
 * Domain: {{domain}}
 * Generated by VytchesDDD CLI
 */

import { describe, it, expect, beforeEach } from 'vitest';
import { safeRun } from '@vytches/ddd-utils';
import { EntityId } from '@vytches/ddd-core';
import { {{name}} } from '../{{fileName}}.entity';

describe('{{name}} Entity', () => {
  let entityId: EntityId;
  {{#each properties}}
  let {{name}}: {{type}};
  {{/each}}

  beforeEach(() => {
    entityId = EntityId.generate();
    {{#each properties}}
    {{name}} = {{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}};
    {{/each}}
  });

  describe('create', () => {
    it('should create {{name}} entity successfully', () => {
      const [error, entity] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      expect(entity).toBeDefined();
      expect(entity?.id).toBeDefined();
      {{#each properties}}
      expect(entity?.{{name}}).toBe({{name}});
      {{/each}}
    });

    {{#if hasValidation}}
    it('should validate entity properties', () => {
      const [error] = safeRun(() => 
        {{name}}.create(
          {{#each properties}}
          {{#if @first}}null as any{{else}}{{name}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeDefined();
      expect(error?.message).toContain('validation');
    });
    {{/if}}
  });

  describe('fromPersistence', () => {
    it('should reconstitute entity from persistence', () => {
      const [error, entity] = safeRun(() => 
        {{name}}.fromPersistence(
          entityId,
          {{#each properties}}
          {{name}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      expect(entity).toBeDefined();
      expect(entity?.id).toBe(entityId);
      {{#each properties}}
      expect(entity?.{{name}}).toBe({{name}});
      {{/each}}
    });
  });

  {{#each methods}}
  describe('{{name}}', () => {
    let entity: {{../name}};

    beforeEach(() => {
      entity = {{../name}}.fromPersistence(
        entityId,
        {{#each ../properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );
    });

    it('should {{description}}', () => {
      const [error{{#unless (eq returnType 'void')}}, result{{/unless}}] = safeRun(() => 
        entity.{{name}}(
          {{#each parameters}}
          {{#if (eq type 'string')}}'test-{{name}}'{{else}}{{#if (eq type 'number')}}123{{else}}{{#if (eq type 'boolean')}}true{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
          {{/each}}
        )
      );

      expect(error).toBeNull();
      {{#unless (eq returnType 'void')}}
      expect(result).toBeDefined();
      {{/unless}}
    });
  });
  {{/each}}

  describe('equality', () => {
    it('should be equal when IDs are the same', () => {
      const entity1 = {{name}}.fromPersistence(
        entityId,
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const entity2 = {{name}}.fromPersistence(
        entityId,
        {{#each properties}}
        {{#if (eq type 'string')}}'different-{{name}}'{{else}}{{#if (eq type 'number')}}456{{else}}{{#if (eq type 'boolean')}}false{{else}}new {{type}}(){{/if}}{{/if}}{{/if}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      expect(entity1.equals(entity2)).toBe(true);
    });

    it('should not be equal when IDs are different', () => {
      const entity1 = {{name}}.fromPersistence(
        entityId,
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const entity2 = {{name}}.fromPersistence(
        EntityId.generate(),
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      expect(entity1.equals(entity2)).toBe(false);
    });
  });

  describe('toSnapshot', () => {
    it('should create snapshot for persistence', () => {
      const entity = {{name}}.fromPersistence(
        entityId,
        {{#each properties}}
        {{name}}{{#unless @last}},{{/unless}}
        {{/each}}
      );

      const snapshot = entity.toSnapshot();

      expect(snapshot).toBeDefined();
      expect(snapshot.id).toBe(entityId.value);
      {{#each properties}}
      expect(snapshot.{{name}}).toBe({{name}});
      {{/each}}
      expect(snapshot.createdAt).toBeDefined();
      expect(snapshot.updatedAt).toBeDefined();
    });
  });
});