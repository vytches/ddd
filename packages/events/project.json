{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@vytches-ddd/events",
  "projectType": "library",
  "sourceRoot": "packages/events/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "vite build",
        "cwd": "packages/events"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run packages/events/tests --reporter=verbose"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/events/**/*.ts"]
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --project packages/events/tsconfig.json"
      }
    }
  },
  "tags": ["type:lib", "scope:events", "layer:architecture"]
}
