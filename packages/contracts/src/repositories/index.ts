/**
 * Repository contracts for dependency inversion
 */

export type {
  IRepositoryEntity,
  IRepository,
  IExtendedRepository,
  IBatchRepository,
  IRepositoryProvider,
  IUnitOfWork,
  IQueryRepository,
  IWriteRepository,
  ICQRSRepository,
} from './repository.interfaces';
