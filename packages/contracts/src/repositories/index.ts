/**
 * Repository contracts for dependency inversion
 */

export type {
  IRepositoryEntity,
  IRepository,
  IExtendedRepository,
  IRepositoryProvider,
  IUnitOfWork,
  IQueryRepository,
  IWriteRepository,
  ICQRSRepository,
} from './repository.interfaces';
