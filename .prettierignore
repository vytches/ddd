# Machine-generated by tools/example-matrix — must byte-match its own
# renderer exactly (example-matrix --check compares committed output
# byte-for-byte); prettier reformatting breaks that check.
docs/coverage-matrix.json
docs/COVERAGE-MATRIX.md

# Machine-generated by API Extractor — the committed baseline must byte-match
# what the tool emits, or the api-surface gate can never go green. lint-staged
# runs `prettier --write` on staged files, which reformats a freshly generated
# report (indentation, wrapping) and makes it mismatch forever. (VF-037)
packages/*/api-report/

# Template files with placeholder tokens
templates/
/.nx/cache
/.nx/workspace-data
.vscode
.vytches-ddd/
dist/
*.cjs
*.mjs
pnpm-lock.yaml

.nx/self-healing